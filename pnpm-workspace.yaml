packages:
  - "apps/*"
  - "packages/*"
  # Expo ilovalari — mustaqil loyihalar (o'z node_modules, npm + EAS bilan
  # quriladi). pnpm workspace'ga kiritilsa EAS `npm ci` bilan yiqiladi.
  - "!apps/driver-app"
  - "!apps/customer-app"
